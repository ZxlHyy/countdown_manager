name: countdown_manager
description: High precision countdown and countup widgets with pause, resume, lifecycle correction, and low-power scheduling.
version: 0.1.0

homepage: https://github.com/ZxlHyy/countdown_manager
repository: https://github.com/ZxlHyy/countdown_manager
issue_tracker: https://github.com/ZxlHyy/countdown_manager/issues
documentation: https://pub.dev/documentation/countdown_manager/latest/
topics:
  - countdown
  - countup
  - timer

screenshots:
  - description: Countdown Manager.
    path: images/countdown_manager.jpg

platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:

environment:
  sdk: ^3.9.2
  flutter: '>=3.3.0'

dependencies:
  flutter:
    sdk: flutter
  system_clock: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
